import {IcircleData} from "../../constants/circleData";
import {eventData, IeventData} from "../../constants/eventData";

export interface ISlider {
    eventData: IeventData[],
    currentIndex: number,
}
